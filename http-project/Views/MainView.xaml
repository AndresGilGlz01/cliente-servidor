<Window x:Class="http_project.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:http_project.Views"
        xmlns:viewModels="clr-namespace:http_project.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Button x:Key="StartServiceButton" Content="Start service" Command="{Binding StartServiceCommand}"/>
        <Button x:Key="StopServiceButton" Content="Stop service" Command="{Binding StopServiceCommand}"/>
    </Window.Resources>
    <StackPanel>
        <TextBlock>
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsServiceRunning}" Value="true">
                            <Setter Property="Text" Value="Is running!"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsServiceRunning}" Value="false">
                            <Setter Property="Text" Value="Is not running!"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <TextBlock Text="{Binding Vms.Message}"/>
        <ContentControl>
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsServiceRunning}" Value="true">
                            <Setter Property="Content" Value="{StaticResource StopServiceButton}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsServiceRunning}" Value="false">
                            <Setter Property="Content" Value="{StaticResource StartServiceButton}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </StackPanel>
</Window>
